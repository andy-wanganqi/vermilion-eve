import { ItemGroup } from '../../../types';
import Astrahus from './Astrahus';
import Fortizar from './Fortizar';

const Citadels: ItemGroup = {
  name: "Citadels",
  subgroups: null,
  items: [
    Astrahus,
    Fortizar
  ],
};

export default Citadels;