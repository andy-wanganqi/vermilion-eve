import { BlueprintGroup } from "../../..";
import StructureComponents from "./StructureComponents";

const ComponentsBlueprints: BlueprintGroup = {
  name: 'Components',
  subgroups: [StructureComponents],
  blueprints: null,
};
export default ComponentsBlueprints;
